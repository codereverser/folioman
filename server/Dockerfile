# Folioman self-hosted server image.
# Placeholder — full build not yet implemented.

FROM python:3.12-slim AS base
WORKDIR /app
# TODO: install uv, copy workspace, install deps, build frontend, collectstatic.
CMD ["echo", "folioman-server: not yet implemented"]
